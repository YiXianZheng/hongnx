<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hongnx.cloud.mall.admin.mapper.EnsureMapper">

    <resultMap id="ensureMap" type="com.hongnx.cloud.mall.common.entity.Ensure">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="createId" column="create_id"/>
        <result property="name" column="name"/>
        <result property="detail" column="detail"/>
    </resultMap>

    <sql id="ensureSql">
        ensure.`id`,        ensure.`tenant_id`,        ensure.`del_flag`,        ensure.`create_time`,        ensure.`update_time`,        ensure.`create_id`,        ensure.`name`,        ensure.`detail`    </sql>
</mapper>
